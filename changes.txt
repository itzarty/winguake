* swapped bell sound for a custom one
* added font resize on `ctrl` + `mwheel`/`+`/`-`
* added timestamp guttter
* added SSH latency detection
* added custom audio engine
* dropped PATH script from NSIS